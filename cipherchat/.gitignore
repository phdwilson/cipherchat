# 依赖与构建产物
node_modules/
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# 本地数据库与运行数据（仅存密文，但不应随发布包分发）
prisma/*.db
prisma/*.db-journal
prisma/*.db-shm
prisma/*.db-wal
data/
uploads/

# 日志
*.log
dev.log
server.log

# 环境变量（含本机配置，发布包只保留 .env.example）
.env.local
.env.*.local

# 编辑器/系统
.DS_Store
Thumbs.db
.idea/
.vscode/

# v1.7.1 数据库路径标记（web 写入，relay 读取，防两进程脑裂）
.cipherchat-db
